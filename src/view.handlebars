<div class="{{classPrefix}}">
  <div class="toolbar" data-role="toolbar">
    {{#each actions}}
      <button class="button-{{role}}" data-role="{{role}}" data-tips="{{tips}}"{{#if disabled}} disabled{{/if}}>{{{text}}}</button>
    {{/each}}
  </div>
  <div class="content" data-role="content"></div>
</div>
